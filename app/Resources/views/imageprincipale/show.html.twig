{% extends 'base.html.twig' %}

{% block body %}
    <h1>Imageprincipale</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ imagePrincipale.id }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ imagePrincipale.path }}</td>
            </tr>
            <tr>
                <th>Alt</th>
                <td>{{ imagePrincipale.alt }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('imageprincipale_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('imageprincipale_edit', { 'id': imagePrincipale.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
